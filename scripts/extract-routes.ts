/**
 * Route extraction script using Kakao Mobility API.
 *
 * Prerequisites:
 *   - Set KAKAO_REST_API_KEY in .env.local
 *
 * Usage:
 *   npm run extract-routes
 *   npm run extract-routes -- --force   # re-extract all
 *
 * This is a stub. For now, use generateMockRoutes.ts instead:
 *   npx tsx scripts/generateMockRoutes.ts
 */

console.log('extract-routes: This is a stub.');
console.log('Use `npx tsx scripts/generateMockRoutes.ts` to generate mock data.');
console.log('To implement real extraction, set KAKAO_REST_API_KEY and update this script.');
